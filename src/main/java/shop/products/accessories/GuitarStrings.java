package shop.products.accessories;
import shop.products.Brand;
import shop.products.Product;

public class GuitarStrings extends Product {

    public GuitarStrings(String description, String SKU, Brand brand, int purchasePrice, int sellingPrice) {
        super(description, SKU, brand, purchasePrice, sellingPrice);
    }
}
